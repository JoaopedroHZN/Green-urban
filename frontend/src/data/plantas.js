/**
 * 🌱 Dados de Plantas
 *
 * Os dados reais agora são obtidos da API backend (http://localhost:4000/api/plantas),
 * que serve o dataset completo de plantasBase.json com 200 espécies do Cerrado.
 *
 * Este arquivo foi mantido para referência de tipagem/estrutura.
 */

const plantas = [];

export default plantas;
